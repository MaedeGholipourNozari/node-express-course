
const compute = () => {
    console.log("Performing a complex calculation...");
    let result = 10 * 5;
    console.log(`Result: ${result}`);
  };
  
  compute(); // This function runs automatically when the module is loaded.
  